spring:
  profiles:
    active: local #default
    group:
      local:
        - common
      prod :
        - common
---
spring:
  config:
    activate:
      on-profile: common



---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/anafine
    username: root
    password: ssafy
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

---
spring:
  config:
    activate:
      on-profile: prod
